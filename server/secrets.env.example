# Copy to secrets.env (gitignored) and fill: openssl rand -base64 24 | tr -d '/+='
# MQTT_DEVICE_PASS must match MQTT_PASS in firmware/main/config/credentials.h
MQTT_DEVICE_PASS=
MQTT_INGEST_PASS=
PG_READ_PASS=
